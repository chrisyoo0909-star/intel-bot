# LLM grading uses a plain OpenAI-compatible REST call (via requests) — no
# vendor SDK required. Default backend is Groq's free API; see analyzer.py.
supabase>=2.4.0
requests>=2.31.0
feedparser>=6.0.11
trafilatura>=1.12.0
streamlit>=1.33.0
python-dotenv>=1.0.1
